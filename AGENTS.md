# AGENTS.md — RTVK 项目规范


## 语言要求

所有回复、注释和文档一律使用**简体中文**。代码标识符和技术术语保持原文。

## 开发规范

- **命名空间**：使用 `VK_NAMESPACE`（即 namespace Vulkan）
- **构建验证**：当前项目不用自动构建。除非用户明确要求，不要主动运行构建命令；优先做文本级、结构级或局部静态检查。
- **注释**：新增功能函数时，添加注释。

### C++ 通用规范

#### 命名规范

- 命名应具体、有意义，尽量避免无必要缩写。
- 类、结构体、枚举、类型别名使用首字母大写的名词或名词短语，例如 `DataManager`、`RenderMesh`。
- 文件名优先与主要类名保持一致，例如 `DataManager.h`、`DataManager.cpp`。
- 工具函数应放在明确的命名空间中，文件名可使用工具命名空间或工具类名，例如 `MathUtils.h`。
- 函数名使用动词或动词短语命名，例如 `GetData()`、`Render()`。
- 虚函数声明必须显式写 `virtual`，覆写虚函数必须显式写 `override`。
- 类成员变量使用 `m_` 前缀，首单词小写，后续单词首字母大写，例如 `m_dataManager`、`m_count`。
- 结构体成员变量不强制使用 `m_` 前缀。
- 局部变量和函数参数使用小驼峰命名，例如 `renderMesh`、`messageSize`。
- 函数输出参数必须加 `out` 前缀，例如 `outResultMesh`。
- 全局变量使用 `g_` 前缀，例如 `g_renderWidget`。
- 静态变量使用 `s_` 前缀，例如 `s_dataMaps`。
- 变量名不要携带类型信息。
- 枚举尽量使用 `enum class` 限定作用范围。
- 枚举值使用首字母大写，不重复枚举名作为前缀，例如使用 `Phong`，不要使用 `ShadingModelPhong`。
- 宏名使用全大写字母，单词之间用 `_` 连接，例如 `GEOMETRY_DLL`。

#### 编码风格

- 头文件中尽量减少 `#include`，优先使用前置声明降低依赖。
- `#include` 顺序：
  1. 当前 `.cpp` 对应的头文件
  2. C 系统头文件
  3. C++ 系统头文件
  4. 第三方库头文件，例如 Qt
  5. 其他库头文件
  6. 本项目头文件
- 使用换行花括号风格。
- 一个函数中，不同功能片段之间使用空行分隔。
- 普通类型强制转换使用 `static_cast<>`。
- 指针类型转换优先使用安全的 C++ 转换方式，例如 `std::dynamic_pointer_cast<>`。
- 不写不必要、冗余的注释。
- 在逻辑复杂、容易混淆或思路不直观的位置添加简洁注释。
- 新增功能函数时添加简短注释，说明函数目的或关键行为。
- 可以使用 `using` 简化很长、重复或容易变化的类型名。

## 编程原则

### 1. 编码前思考

**不要假设。不要隐藏困惑。呈现权衡。**

LLM 经常默默选择一种解释然后执行。这个原则强制明确推理：

- **明确说明假设** — 如果不确定，询问而不是猜测
- **呈现多种解释** — 当存在歧义时，不要默默选择
- **适时提出异议** — 如果存在更简单的方法，说出来
- **困惑时停下来** — 指出不清楚的地方并要求澄清

### 2. 简洁优先

**用最少的代码解决问题。不要过度推测。**

对抗过度工程的倾向：

- 不要添加要求之外的功能
- 不要为一次性代码创建抽象
- 不要添加未要求的"灵活性"或"可配置性"
- 不要为不可能发生的场景做错误处理
- 如果 200 行代码可以写成 50 行，重写它

**检验标准：** 资深工程师会觉得这过于复杂吗？如果是，简化。

### 3. 职责拆分

**长流程要拆清职责。**

复杂流程不要全部堆进一个文件、一个类或一个超长函数里。

- 当流程包含多个稳定阶段、独立状态、外部依赖或可复用协作逻辑时，应拆分为职责明确的类或组件。
- 当逻辑只是单一计算、转换、校验、查找或构造，且无独立状态和生命周期时，优先使用普通函数、私有函数。
- 类应该代表一个清晰的功能角色，而不是函数的容器。
- 拆分后的代码应让主流程更短、更容易读，并且让每个类或函数的修改原因更单一。
- 如果拆分类会导致阅读者在更多文件之间跳转，却没有减少复杂度，就不要拆。
- 不要把一次性流程提前设计成可扩展框架；先满足当前明确需求。

**检验标准：** 这个新类是否有独立职责、状态或协作边界？如果只是为了放一个函数，保持为函数。

### 4. 精准修改

**只碰必须碰的。只清理自己造成的混乱。**

编辑现有代码时：

- 不要"改进"相邻的代码、注释或格式
- 不要重构没坏的东西
- 匹配现有风格，即使你更倾向于不同的写法
- 如果注意到无关的死代码，提一下 —— 不要删除它

当你的改动产生孤儿代码时：

- 删除因你的改动而变得无用的导入/变量/函数
- 不要删除预先存在的死代码，除非被要求

**检验标准：** 每一行修改都应该能直接追溯到用户的请求。



## 忽略目录

以下一级目录不单独建立开发规范：

- `.github/`
- `.claude/`
- `build/`
- `Cmake/`
- `ApplicationData/`




## 项目约定

- C++17，CMake 3.25+，Qt 6.11，Vulkan 1.3
- 模块：`src/core` / `src/sim` / `src/render` / `src/ui`
- 构建：`cmake --build build --config Debug`
- 运行前设置 PATH：`$env:PATH = "C:\Qt\6.11.1\msvc2022_64\bin;$env:PATH"`
- 不要提交 `vk.log`、`build/`、`*.spv`